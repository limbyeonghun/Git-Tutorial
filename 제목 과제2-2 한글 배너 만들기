PFont f;
void setup(){
 size(800,500);
 textSize(72);
 f = createFont("굴림",32);
 textFont(f);
}
int i, a=1;
void draw(){
 fill(0);
 background(255,50,255);
 if(keyPressed)
  a = key-'0';
 text("안동대 컴공 사랑합니다",250,i=i+a ,250,250);
 if (i>800) i=0;
} 

작성소감: 처음부터 지금까지 배운것들을 연계하여서 합쳤을때 이런한 결과물이 나온다는 것이 신기하고 앞으로도 얼마나 많은
코드들을 배울지 기대가 됩니다 !
